# Medaka Simulation (メダカ放流シミュレーション) プロジェクト概要

## プロジェクトの目的
野生のメダカ（ブラウン）が住む池に、観賞用に交配された改良メダカ（オレンジ色など）を放流した際に起こる「遺伝子汚染」や生態系の破壊を視覚的に、かつ数学的・生物学的に正確にシミュレーションするWebアプリケーションです。

## 技術スタック
- **Frontend Framework**: React + Vite
- **Styling**: Vanilla CSS (`MedakaSim.css`, `index.css`)
- **Assets Processing**: Python (`make_transparent.py`, `make_transparent_outer.py`) を用いた透過画像生成スクリプト

## アプリケーションの動作と仕様 (Core Logic)
メインロジックは `src/MedakaSim.jsx` に集約されています。

### 1. 遺伝子型と表現型 (Genotype & Phenotype)
- **優性 (A): 野生, 劣性 (a): 改良**
- **AA (野生)**: 茶色。生存力が高い基準。
- **Aa (交雑)**: 茶色。見た目は野生だが、被食率などの生存ペナルティを少し受ける。
- **aa (改良)**: オレンジ、黒、青など。自然界で目立つため被食率が非常に高い。

### 2. イベントごとの生存・繁殖判定 (Generational Overlap Model)
1年に1回サイクルが回り、オーバーラップモデル（親も生き残って次世代へ進む）を採用しています。
- **春 (繁殖)**: ハーディ・ワインベルクの法則 (Hardy-Weinberg principle) に基づき、交配を行います。
- **夏〜秋 (被食)**: 天敵による捕食プレッシャーです。池の過密状態（環境収容力：約50〜100匹）に応じてペナルティが変動します。
- **冬 (病気・老衰)**: 
  - 全個体一律15%の病気による死亡判定。
  - 病気を生き延びた個体にのみ適用される年齢ごとの老衰判定（0歳=0%, 1歳=10%, 2歳=20%, 3歳=65%, 4歳=100%）。

### 3. アルゴリズムの工夫 (ハイブリッド方式)
運要素（ガチャ）による極端な絶滅・増殖を防ぐため、計算ベース（決定論的）の割合とランダム制の割合を混ぜ合わせたハイブリッド方式を採用しています。
- **繁殖**: 決定論的90%、ガチャ10%（最大剰余方式を利用してキッチリ配分）
- **死・被食**: 決定論的60〜70%、ガチャ30〜40%（期待値の分配とガチャを併用）
これにより、生物学上の「大数の法則」を小規模なシミュレーションでも機能させて安定した人口推移を体感させます。

## 現在の実装状況とファイルの役割
- `src/App.jsx`: エントリーポイント。`MedakaSim`コンポーネントを呼び出す。
- `src/MedakaSim.jsx`: アプリの本体。状態管理（setup/simulating）、シミュレーションの計算ロジック、アニメーション付きのUIと推移グラフの描画。
- `src/MedakaSim.css`: アプリ全体のスタイリング。
- `src/assets/`: 透過処理済みのメダカ画像たち（野生・改良各種）。
- `make_transparent.py`系: 画像の白背景や特定の背景色をアルファ透過させる処理スクリプト群。

## 今後の再開時に向けたTo-Do (アイデア)
- **パラメータの微調整**: ユーザーからのプレッシャーバランス（被食率、寿命）に関するフィードバックに基づいた手直し。
- **UI/UX向上**: グラフの追加情報、設定画面での細かいパラメータ（環境収容力、死亡率など）の可変機能追加。
- **モバイル対応**: スマートフォンなど小さい画面での池の表示とグラフの調整。
- **エクスポート機能**: シミュレーション結果のCSV書き出しなど。

---
**※ このドキュメントは、プロジェクトを一時中断し、後日いつでも開発を再開できるように現在の理解と状態をまとめたものです。**
